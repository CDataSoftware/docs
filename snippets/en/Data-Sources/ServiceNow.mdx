import SaveAndTest from '/snippets/en/save-and-test.mdx';

## Setup Guide

Follow these steps to connect ServiceNow to your Connect AI account:

<Steps>
<Step>
Open the **Connections** page of the Connect AI dashboard.
</Step>
<Step>
Click **\+ Add Connection** in the upper-right corner.
</Step>
<Step>
Type *ServiceNow* into the search field, then click the data source name.
</Step>
<Step>
On the **Basic Settings** tab of the new connection, enter a connection name or keep the default name.
</Step>
<Step>
Enter the base **URL** of your ServiceNow instance (for example, `https://MyInstance12345.service-now.com/`).
</Step>
<Step>
Select the Authentication method, then proceed to the relevant section and follow those instructions.
</Step>
</Steps>

## Authentication Methods

<Tabs>
  <Tab title="Basic">
<Steps>
<Step>
Enter the ServiceNow username for authentication in the **User** Field.
</Step>
<Step>
In **Password**, enter the user password.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="OAuth">
<Steps>
<Step>
Enter the **OAuth Client Id** in your app settings.
</Step>
<Step>
Enter the **OAuth Client Secret** in your app settings.
</Step>
<Step>
Click **Sign in** to connect securely through OAuth. This action opens the ServiceNow sign-in page in a new tab.
</Step>
<Step>
Log in to your ServiceNow account and provide the requested permissions (if applicable).
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="OauthPassword">
<Steps>
<Step>
Enter the ServiceNow username for authentication in the **User** Field.
</Step>
<Step>
In **Password**, enter the user password.
</Step>
<Step>
Enter the **OAuth Client Id** in your app settings.
</Step>
<Step>
Enter the **OAuth Client Secret** in your app settings.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="OKTA">
<Steps>
<Step>
Enter the OKTA **User** Id.
</Step>
<Step>
Add the **Password** for that account.
</Step>
<Step>
Set the **SSO Login URL** to the URL use by the SSO provider.
</Step>
<Step>
Enter the **SSO Properties**, with the format 'ssoproperty1=value1;sooproperty2=value2;sooproperty3=value3;'. Make sure to separate all property-value pairs with semicolons.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="ADFS">
<Steps>
<Step>
Enter the ADFS **User** Id.
</Step>
<Step>
Add the **Password** for that account.
</Step>
<Step>
Set the **SSO Login URL** to the URL use by the SSO provider.
</Step>
<Step>
Enter the **SSO Properties**, with the format 'ssoproperty1=value1;sooproperty2=value2;sooproperty3=value3;'. Make sure to separate all property-value pairs with semicolons.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="OneLogin">
<Steps>
<Step>
Enter the OneLogin **User** Id.
</Step>
<Step>
Add the **Password** for that account.
</Step>
<Step>
Enter the **SSO Properties**, with the format 'ssoproperty1=value1;sooproperty2=value2;sooproperty3=value3;'. Make sure to separate all property-value pairs with semicolons.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="PingFederate">
<Steps>
<Step>
Enter the PingFederate **User** Id.
</Step>
<Step>
Add the **Password** for that account.
</Step>
<Step>
Set the **SSO Login URL** to the URL use by the SSO provider.
</Step>
<Step>
Enter the **SSO Properties**, with the format 'ssoproperty1=value1;sooproperty2=value2;sooproperty3=value3;'. Make sure to separate all property-value pairs with semicolons.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>
</Tabs>
## More Information

For more information about interactions between Connect AI and ServiceNow, see [this information page](https://cdn.cdata.com/help/BNK/cloud/default.htm#default).