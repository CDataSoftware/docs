import SaveAndTest from '/en/snippets/save-and-test.mdx';

## Setup Guide

Follow these steps to connect Azure Synapse to your Connect AI account:

<Steps>
<Step>
Open the **Connections** page of the Connect AI dashboard.
</Step>
<Step>
Click **\+ Add Connection** in the upper-right corner.
</Step>
<Step>
Type *Azure Synapse* into the search field, then click the data source name.
</Step>
<Step>
On the **Basic Settings** tab of the new connection, enter a connection name or keep the default name.
</Step>
</Steps>

## Authentication Methods

<Tabs>
  <Tab title="Password">
<Steps>
<Step>
Enter the following information:
	- The **User** and **Password** for Azure Synapse.
	- **Port**—the port for connecting to the Azure Synapse server.
	- **Server**—the host name or IP address of the Azure Synapse server.
	- **Database**—(optional) the name of the Azure Synapse database.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="AzureAD">
<Steps>
<Step>
Enter the following information:
	- **Port**—the port for connecting to the Azure Synapse server.
	- **Server**—the host name or IP address of the Azure Synapse server.
	- **Database**—(optional) the name of the Azure Synapse database.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="AzureServicePrincipal">
<Steps>
<Step>
Enter the following information:
	- **Azure Tenant**—the Microsoft Online tenant that you use to access data.
	- **OAuth Client Id**—the Id assigned when you registered your Azure Synapse account for OAuth.
	- Either the **OAuth Client Secret** for your Azure Synapse account for OAuth OR the **OAuth JWT Cert** multi-line key in PEM format. Follow the format shown.
	- **Port**—the port for connecting to the Azure Synapse server.
	- **Server**—the host name or IP address of the Azure Synapse server.
	- **Database**—(optional) the name of the Azure Synapse database.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>
</Tabs>
## More Information

For more information about interactions between Connect AI and Azure Synapse, see [this information page](https://cdn.cdata.com/help/HEM/cloud/default.htm#default).