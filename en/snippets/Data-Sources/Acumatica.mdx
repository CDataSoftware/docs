import SaveAndTest from '/en/snippets/save-and-test.mdx';

## Setup Guide

Follow these steps to connect Acumatica to your Connect AI account:

<Steps>
<Step>
Open the **Connections** page of the Connect AI dashboard.
</Step>
<Step>
Click **\+ Add Connection** in the upper-right corner.
</Step>
<Step>
Type *Acumatica* into the search field, then click the data source name.
</Step>
<Step>
On the **Basic Settings** tab of the new connection, enter a connection name or keep the default name.
</Step>
<Step>
Enter the base **URL** of your Acumatica site.
</Step>
<Step>
Select the Authentication method, then proceed to the relevant section and follow those instructions.
</Step>
</Steps>

## Authentication Methods

<Tabs>
  <Tab title="Basic">
<Steps>
<Step>
Enter the Acumatica username for authentication in the **User** field.
</Step>
<Step>
In the **Password** field, enter the user password.
</Step>
<Step>
Select a **Schema**. By default, this is set to `REST` Contract-Based API. You can also select `OData` to use the OData API for Acumatica Generic Inquiries. See the [OData](/en/API/OData-Dashboard) page for more information.
</Step>
<Step>
If using the `REST` API, enter your **Endpoint Name** and **Endpoint Version**. These values can be found under Endpoint Properties in the Web Services Endpoint form of your Acumatica ERP.
</Step>
<Step>
If using the `Odata` API, enter your **Company** Name.
</Step>
<SaveAndTest title="Acumatica"/>
</Steps>
  </Tab>

  <Tab title="OAuth">
<Steps>
<Step>
Enter the **OAuth Client Id** assigned when you registered your Acumatica account.
</Step>
<Step>
Enter the **OAuth Client Secret** for your Acumatica account.
</Step>
<Step>
Select a **Schema**. By default, this is set to `REST` Contract-Based API. You can also select `OData` to use the OData API for Acumatica Generic Inquiries.
</Step>
<Step>
If using the `REST` API, enter your **Endpoint Name** and **Endpoint Version**. These values can be found under Endpoint Properties in the Web Services Endpoint form of your Acumatica ERP.
</Step>
<Step>
If using the `Odata` API, enter your **Company** Name.
</Step>
<Step>
Click **Sign in** to connect securely through OAuth. This action opens the Acumatica sign-in page in a new tab.
</Step>
<Step>
Log in to your Acumatica account and provide the requested permissions (if applicable).
</Step>
<SaveAndTest title="Acumatica"/>
</Steps>
  </Tab>
</Tabs>
## More Information

For more information about interactions between Connect AI and Acumatica, see [this information page](https://cdn.cdata.com/help/OIM/cloud/default.htm#default).