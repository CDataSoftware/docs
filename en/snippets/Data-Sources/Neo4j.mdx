import SaveAndTest from '/en/snippets/save-and-test.mdx';

## Setup Guide

Follow these steps to connect Neo4j to your Connect AI account:

<Steps>
<Step>
Open the **Connections** page of the Connect AI dashboard.
</Step>
<Step>
Click **\+ Add Connection** in the upper-right corner.
</Step>
<Step>
Type *Neo4j* into the search field, then click the data source name.
</Step>
<Step>
On the **Basic Settings** tab of the new connection, enter a connection name or keep the default name.
</Step>
<Step>
Enter the **Server** hosting the Neo4j instance. You may optionally prepend a HTTP or HTTPS protocol prefix to the server address or name you specify, for example, https://127.0.0.1. The default value is 127.0.0.1.
</Step>
<Step>
Enter the **Port** used for connecting to the Neo4j server. The default value is 7474. If connecting over HTTPS, supply the HTTPS port for the Neo4j instance specified in the **Server** field.
</Step>
<Step>
Enter the **API** to connect to in your Neo4j instance, **Query** or **HTTP**.
</Step>
<Step>
Enter the **User** Id for the authenticating user.
</Step>
<Step>
Enter the **Password** for the authenticating user.
</Step>
<Step>
Enter the **Database** to target in the Neo4j instance. The default value is Neo4j.
</Step>
<SaveAndTest title="Neo4j"/>
</Steps>
## More Information

For more information about interactions between Connect AI and Neo4j, see [this information page](https://cdn.cdata.com/help/LNK/cloud/default.htm#default).