import SaveAndTest from '/ja/snippets/save-and-test.mdx';

## セットアップガイド

以下の手順で、SharePoint をConnect AI アカウントに接続します。

<Steps>
<Step>
Connect AI のダッシュボードから**Connections** ページを開きます。
</Step>
<Step>
右上の **+ Add Connection** をクリックします。
</Step>
<Step>
検索フィールドに *SharePoint* と入力し、データソース名をクリックします。
</Step>
<Step>
新しいコネクションの**Basic Settings** タブで、コネクション名を入力するかデフォルトの名前をそのまま使用します。
</Step>
<Step>
Enter the base **URL** for the SharePoint site (for example, `https://teams.contoso.com`).
</Step>
<Step>
Select the **Share Point Edition** from the drop down.
</Step>
<Step>
Select the Authentication method, then proceed to the relevant section and follow those instructions.
</Step>
</Steps>

### AzureAD

<Steps>
<Step>
Choose REST or SOAP from the **Schema** drop-down list.
</Step>
<Step>
OAuth でセキュアに接続するには、**Sign in** をクリックします。これにより、SharePoint のサインインページが新しいタブで開かれます。
</Step>
<Step>
SharePoint アカウントにログインし、リクエストされた権限を設定します（該当する場合）。
</Step>
<SaveAndTest/>
</Steps>

### AzurePassword

<Steps>
<Step>
Enter your **User** Id.
</Step>
<Step>
Add the **Password** for that account.
</Step>
<Step>
Enter the **Azure Tenant** Id found on the Overview page of the OAuth app used to authenticate to Microsoft SharePoint on Azure.
</Step>
<Step>
Choose REST or SOAP from the **Schema** drop-down list.
</Step>
<SaveAndTest/>
</Steps>

## Authentication Methods


<Tabs>
  <Tab title="OAuth JWT">
<Steps>
<Step>
Enter the **Azure Tenant** Id found on the Overview page of the OAuth app used to authenticate to Microsoft SharePoint on Azure.
</Step>
<Step>
Enter your **OAuth JWT Certificate**. This is multi-line and in PEM format.
</Step>
<Step>
Select the **OAuth JWT Cert type**.
</Step>
<Step>
Enter the **OAuth JWT Issuer**. This should be set to the OAuth client Id.
</Step>
<Step>
Choose REST or SOAP from the **Schema** drop-down list.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="PingFederate">
<Steps>
<Step>
Enter the PingFederate **User** Id.
</Step>
<Step>
Add the **Password** for that account.
</Step>
<Step>
Enter the **SSO Domain** to connect to.
</Step>
<Step>
Choose REST or SOAP from the **Schema** drop-down list.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="ADFS">
<Steps>
<Step>
Enter the ADFS **User** Id.
</Step>
<Step>
Add the **Password** for that account.
</Step>
<Step>
Enter the **SSO Domain** to connect to.
</Step>
<Step>
Choose REST or SOAP from the **Schema** drop-down list.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="OneLogin">
<Steps>
<Step>
Enter the OneLogin **User** Id.
</Step>
<Step>
Add the **Password** for that account.
</Step>
<Step>
Enter the **SSO Domain** to connect to.
</Step>
<Step>
Choose REST or SOAP from the **Schema** drop-down list.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="OKTA">
<Steps>
<Step>
Enter the OKTA **User** Id.
</Step>
<Step>
Add the **Password** for that account.
</Step>
<Step>
Enter the **SSO Domain** to connect to.
</Step>
<Step>
Choose REST or SOAP from the **Schema** drop-down list.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>
</Tabs>
## 詳細について

Connect AI とSharePoint の連携の詳細動作については、[こちらのページ](https://cdn.cdata.com/help/RSM/ja/cloud/default.htm#default)を参照してください。