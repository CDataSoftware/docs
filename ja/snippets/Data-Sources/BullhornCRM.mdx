import SaveAndTest from '/ja/snippets/save-and-test.mdx';

## セットアップガイド

以下の手順で、Bullhorn CRM をConnect AI アカウントに接続します。

<Steps>
<Step>
Connect AI のダッシュボードから**Connections** ページを開きます。
</Step>
<Step>
右上の **+ Add Connection** をクリックします。
</Step>
<Step>
検索フィールドに *Bullhorn CRM* と入力し、データソース名をクリックします。
</Step>
<Step>
新しいコネクションの**Basic Settings** タブで、コネクション名を入力するかデフォルトの名前をそのまま使用します。
</Step>
<Step>
Enter the **Data Center Code** where your account data is hosted. This code appears at the beginning of the Bullhorn CRM URL when you are logged in. For example, if your URL is `https://cls21.bullhornstaffing.com/BullhornSTAFFING/MainFrame.jsp`, your code is `CLS21`.
</Step>
<Step>
Contact Bullhorn CRM to set up a custom OAuth application with the correct redirect URI for Connect AI, which is *`https://oauth.cdata.com/oauth/`*.
	<Note>
	 **Note:** Bullhorn CRM does not allow users to configure custom OAuth applications themselves. You must contact Bullhorn CRM for this step.
	</Note>
</Step>
<Step>
After setting up your OAuth application, enter your **OAuth Client Id** and **OAuth Client Secret** in Connect AI.
</Step>
<Step>
Select an **Auth Scheme** and follow the relevant instructions below.
</Step>
</Steps>

## Authentication Methods


<Tabs>
  <Tab title="OAuth">
<Steps>
<Step>
Toggle **Include Custom Fields** on or off, depending on whether you want to include custom fields in the column listing.
</Step>
<Step>
OAuth でセキュアに接続するには、**Sign in** をクリックします。これにより、Bullhorn CRM のサインインページが新しいタブで開かれます。
</Step>
<Step>
Bullhorn CRM アカウントにログインし、リクエストされた権限を設定します（該当する場合）。
</Step>
<SaveAndTest title="Bullhorn CRM"/>
</Steps>
  </Tab>

  <Tab title="OAuthPassword">
<Steps>
<Step>
Enter 認証用のBullhorn CRM ユーザー名.
</Step>
<Step>
Enter ユーザーパスワード.
</Step>
<Step>
Toggle **Include Custom Fields** on or off, depending on whether you want to include custom fields in the column listing.
</Step>
<SaveAndTest title="Bullhorn CRM"/>
</Steps>
  </Tab>
</Tabs>
## 詳細について

Connect AI とBullhorn CRM の連携の詳細動作については、[こちらのページ](https://cdn.cdata.com/help/HAM/ja/cloud/default.htm#default)を参照してください。