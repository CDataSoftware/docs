import SaveAndTest from '/ja/snippets/save-and-test.mdx';

## Prerequisites

Before you can establish a connection with Docusign using the OAuth authentication method, you must create and configure a custom OAuth application.

<Steps>
<Step>
In Docusign, navigate to **Settings** > **Integrations** > **Apps and Keys**.
</Step>
<Step>
Click **Add App and Integration Key** and name your application.
</Step>
<Step>
Click **Create App**.
</Step>
<Step>
In **General Info**, note the **Integration Key** field. You need to copy and paste this key into the **OAuthClientId** field in Connect AI.
</Step>
<Step>
In **Authentication** > **Secret Keys**, click **Add Secret Key**. You need to copy and paste this key into the **OAuth Client Secret** field in Connect AI..
</Step>
<Step>
In **Additional Settings** > **Redirect URIs**, click **Add URI**. Enter `https://oauth.cdata.com/oauth/` as the redirect URI.
</Step>
<Step>
(Optional) If you are using JWT authentication, in **Service Integration** > **RSA Keypairs**, click **Generate RSA** to generate an RSA public key and private key. You need to copy and paste the private key into the **OAuth JWT Cert** field in Connect AI.
</Step>
<Step>
(Optional) If you are using JWT authentication, in the **Apps and Keys** home page, note **My Account Information** > **User ID**. You need to copy and paste this key into the **OAuth JWT Subject** field in Connect AI.
</Step>
</Steps>

Refer to the [Docusign documentation](https://developers.docusign.com/platform/build-integration/) for more details about building a Docusign integration.

## セットアップガイド

以下の手順で、Docusign をConnect AI アカウントに接続します。

<Steps>
<Step>
Connect AI のダッシュボードから**Connections** ページを開きます。
</Step>
<Step>
右上の **+ Add Connection** をクリックします。
</Step>
<Step>
検索フィールドに *Docusign* と入力し、データソース名をクリックします。
</Step>
<Step>
新しいコネクションの**Basic Settings** タブで、コネクション名を入力するかデフォルトの名前をそのまま使用します。
</Step>
<Step>
Select an **AuthScheme** and follow the relevant instructions below.
</Step>
</Steps>

## Authentication Methods


<Tabs>
  <Tab title="OAuth">
<Steps>
<Step>
For **OAuth Client Id**, copy and paste the Docusign integration key you created in the prerequisites.
</Step>
<Step>
For **OAuth Client Secret**, copy and paste the secret key you created in the prerequisites.
</Step>
<Step>
The **Callback URL** is the redirect URI for your OAuth app. On Connect AI, this is `https://oauth.cdata.com/oauth/`.
</Step>
<Step>
OAuth でセキュアに接続するには、**Sign in** をクリックします。これにより、Docusign のサインインページが新しいタブで開かれます。
</Step>
<Step>
Docusign アカウントにログインし、リクエストされた権限を設定します（該当する場合）。
</Step>
<SaveAndTest title="Docusign"/>
</Steps>
  </Tab>

  <Tab title="OAuthJWT">
<Steps>
<Step>
For **OAuth JWT Cert**, copy and paste the RSA private key you generated in the prerequisites.
</Step>
<Step>
For **OAuth JWT Subject**, copy and paste your Docusign user Id. Retrieve your user Id in Docusign by navigating to **Settings** > **Apps and Keys** > **My Account Information** > **User ID**.
</Step>
<Step>
For **OAuth JWT Issuer**, copy and paste the Docusign integration key you created in the prerequisites.
</Step>
<SaveAndTest title="Docusign"/>
</Steps>
  </Tab>
</Tabs>
## 詳細について

Connect AI とDocusign の連携の詳細動作については、[こちらのページ](https://cdn.cdata.com/help/UCM/jp/cloud/default.htm#default)を参照してください。