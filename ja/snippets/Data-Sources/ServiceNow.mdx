import SaveAndTest from '/ja/snippets/save-and-test.mdx';

## セットアップガイド

以下の手順で、ServiceNow をConnect AI アカウントに接続します。

<Steps>
<Step>
Connect AI のダッシュボードから**Connections** ページを開きます。
</Step>
<Step>
右上の **+ Add Connection** をクリックします。
</Step>
<Step>
検索フィールドに *ServiceNow* と入力し、データソース名をクリックします。
</Step>
<Step>
新しいコネクションの**Basic Settings** タブで、コネクション名を入力するかデフォルトの名前をそのまま使用します。
</Step>
<Step>
Enter the base **URL** of your ServiceNow instance (for example, `https://MyInstance12345.service-now.com/`).
</Step>
<Step>
Select the Authentication method, then proceed to the relevant section and follow those instructions.
</Step>
</Steps>

## Authentication Methods


<Tabs>
  <Tab title="Basic">
<Steps>
<Step>
Enter 認証用のServiceNow ユーザー名 in the **User** Field.
</Step>
<Step>
In **Password**, enter ユーザーパスワード.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="OAuth">
<Steps>
<Step>
Enter the **OAuth Client Id** in your app settings.
</Step>
<Step>
Enter the **OAuth Client Secret** in your app settings.
</Step>
<Step>
OAuth でセキュアに接続するには、**Sign in** をクリックします。これにより、ServiceNow のサインインページが新しいタブで開かれます。
</Step>
<Step>
ServiceNow アカウントにログインし、リクエストされた権限を設定します（該当する場合）。
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="OauthPassword">
<Steps>
<Step>
Enter 認証用のServiceNow ユーザー名 in the **User** Field.
</Step>
<Step>
In **Password**, enter ユーザーパスワード.
</Step>
<Step>
Enter the **OAuth Client Id** in your app settings.
</Step>
<Step>
Enter the **OAuth Client Secret** in your app settings.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="OKTA">
<Steps>
<Step>
Enter the OKTA **User** Id.
</Step>
<Step>
Add the **Password** for that account.
</Step>
<Step>
Set the **SSO Login URL** to the URL use by the SSO provider.
</Step>
<Step>
Enter the **SSO Properties**, with the format 'ssoproperty1=value1;sooproperty2=value2;sooproperty3=value3;'. Make sure to separate all property-value pairs with semicolons.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="ADFS">
<Steps>
<Step>
Enter the ADFS **User** Id.
</Step>
<Step>
Add the **Password** for that account.
</Step>
<Step>
Set the **SSO Login URL** to the URL use by the SSO provider.
</Step>
<Step>
Enter the **SSO Properties**, with the format 'ssoproperty1=value1;sooproperty2=value2;sooproperty3=value3;'. Make sure to separate all property-value pairs with semicolons.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="OneLogin">
<Steps>
<Step>
Enter the OneLogin **User** Id.
</Step>
<Step>
Add the **Password** for that account.
</Step>
<Step>
Enter the **SSO Properties**, with the format 'ssoproperty1=value1;sooproperty2=value2;sooproperty3=value3;'. Make sure to separate all property-value pairs with semicolons.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>

  <Tab title="PingFederate">
<Steps>
<Step>
Enter the PingFederate **User** Id.
</Step>
<Step>
Add the **Password** for that account.
</Step>
<Step>
Set the **SSO Login URL** to the URL use by the SSO provider.
</Step>
<Step>
Enter the **SSO Properties**, with the format 'ssoproperty1=value1;sooproperty2=value2;sooproperty3=value3;'. Make sure to separate all property-value pairs with semicolons.
</Step>
<SaveAndTest/>
</Steps>
  </Tab>
</Tabs>
## 詳細について

Connect AI とServiceNow の連携の詳細動作については、[こちらのページ](https://cdn.cdata.com/help/BNM/ja/cloud/default.htm#default)を参照してください。